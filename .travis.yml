language: python
python:
  - "2.7"
branches:
  only:
  - master
script: python crawler.py
deploy:
  provider: pages:git
  token: $GITHUB_TOKEN
  edge: true
  skip_cleanup: true  
  keep_history: true
  on:
    branch: master
install:
- pip install -r requirements.txt
